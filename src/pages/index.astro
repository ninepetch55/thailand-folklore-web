<section class="global-stage-v36">
    <div class="container-v36">
        <div class="header-v36">
            <h2 class="title-gold">Global Stage</h2>
            <div class="gold-line"></div>
            <span class="sub-text">80% Outbound</span>
        </div>
        
        <div class="sovereign-grid">
            {[1, 2, 3, 4, 5, 6, 7, 8].map((i) => (
                <div class={`grid-card v36-item-${i} ${i === 1 || i === 6 ? 'wide' : ''}`}>
                    <img src={`/img/g${i}.jpg`} class="card-img" alt={`Project ${i}`} />
                    <div class="card-hover">
                        <span class="italic-text">View Project</span>
                    </div>
                </div>
            ))}
        </div>
    </div>
</section>

<style>
    /* 🛠️ ล็อคสีพื้นหลังและระยะห่าง */
    .global-stage-v36 { 
        padding: 120px 0; 
        background: #001F3F; /* สีน้ำเงินเข้มตามสเปก */
        width: 100%;
        overflow: hidden;
    }
    .container-v36 { max-width: 1200px; margin: 0 auto; padding: 0 20px; }
    
    .header-v36 { display: flex; align-items: center; gap: 20px; margin-bottom: 60px; }
    .title-gold { font-family: serif; font-style: italic; font-size: 3rem; color: #D4AF37; margin: 0; }
    .gold-line { flex-grow: 1; height: 1px; background: rgba(212, 175, 55, 0.2); }
    .sub-text { color: rgba(255, 255, 255, 0.4); font-size: 10px; letter-spacing: 0.4em; text-transform: uppercase; }

    /* 🛡️ ป้องกันรูปกระจายด้วย CSS Grid ที่แข็งแรง */
    .sovereign-grid {
        display: grid;
        grid-template-columns: repeat(4, 1fr); /* ล็อคไว้ที่ 4 คอลัมน์ */
        gap: 15px;
        width: 100%;
    }

    .grid-card { 
        position: relative; 
        height: 300px; /* ล็อคความสูงเท่ากันทุกใบ */
        overflow: hidden; 
        background: #000;
        border: 1px solid rgba(212, 175, 55, 0.1);
    }
    
    /* ล็อคตำแหน่งภาพกว้างเป็นพิเศษ */
    .grid-card.wide { grid-column: span 2; }

    .card-img { 
        width: 100%; 
        height: 100%; 
        object-fit: cover; /* บังคับให้ภาพพอดีกล่อง ไม่เบี้ยว */
        transition: 0.8s ease; 
    }
    
    .card-hover {
        position: absolute; inset: 0; background: rgba(0, 31, 63, 0.8);
        display: flex; align-items: center; justify-content: center;
        opacity: 0; transition: 0.4s;
    }
    .grid-card:hover .card-hover { opacity: 1; }
    .grid-card:hover .card-img { transform: scale(1.1); }
    .italic-text { color: #D4AF37; font-family: serif; font-style: italic; font-size: 1.2rem; }

    /* ปรับแต่งสำหรับมือถือ */
    @media (max-width: 768px) {
        .sovereign-grid { grid-template-columns: repeat(2, 1fr); }
        .grid-card.wide { grid-column: span 2; }
        .grid-card { height: 200px; }
    }
</style>