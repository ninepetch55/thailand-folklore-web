---
// WDM Note: ตรวจสอบว่ามีไฟล์รูปใน public/img/ จริงๆ นะครับ
---
<section class="hero-sovereign">
  <div class="hero-container">
    <div class="hero-visual">
      <div class="glow-aura"></div>
      <p class="hero-tag">THE SOVEREIGN DIGITAL HERITAGE</p>
      <h1 class="hero-title font-serif italic">Thailand <br/> <span class="gold-text">Folklore</span></h1>
    </div>

    <div class="hero-categories">
      <div class="cat-item">
        <span class="num">01</span>
        <div class="cat-info">
          <h3>Verified Categories</h3>
          <p>Series / 26</p>
        </div>
      </div>
      <div class="cat-item">
        <span class="num">02</span>
        <div class="cat-info">
          <h3>Digital Artifacts</h3>
          <p>Archive / 26</p>
        </div>
      </div>
      <div class="cat-item">
        <span class="num">03</span>
        <div class="cat-info">
          <h3>Linked Metadata</h3>
          <p>Nodes / 28</p>
        </div>
      </div>
    </div>
  </div>
</section>

<style>
  .hero-sovereign {
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 120px 40px 60px 40px !important; /* จัดลำดับ Padding ใหม่ */
    background: transparent;
    position: relative;
    z-index: 20;
  }
  .hero-container { max-width: 1400px; width: 100%; text-align: center; }
  
  .hero-tag { font-size: 12px; letter-spacing: 0.8em; color: #bf953f; font-weight: bold; margin-bottom: 30px; opacity: 1; }
  .hero-title { font-size: clamp(4rem, 10vw, 8rem); line-height: 1; color: #1a1a1a; margin: 0; }
  .gold-text { color: #bf953f; }

  .hero-categories {
    display: flex;
    justify-content: center;
    gap: 80px;
    margin-top: 100px;
    border-top: 1px solid rgba(191,149,63,0.1);
    padding-top: 50px;
  }
  .cat-item { display: flex; align-items: flex-start; gap: 15px; text-align: left; }
  .num { font-size: 11px; color: #bf953f; font-weight: bold; margin-top: 5px; }
  .cat-info h3 { font-size: 1.2rem; margin: 0; color: #1a1a1a; font-family: serif; font-style: italic; }
  .cat-info p { font-size: 10px; text-transform: uppercase; color: #999; letter-spacing: 1px; margin: 5px 0 0 0; }

  @media (max-width: 1024px) {
    .hero-categories { flex-direction: column; gap: 40px; align-items: center; text-align: center; }
  }
</style>