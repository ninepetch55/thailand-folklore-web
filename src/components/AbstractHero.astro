---
// ดึงรูปภาพ h1 - h6 จาก public/img/
const images = [
  "/img/h1.jpg", 
  "/img/h2.jpg", 
  "/img/h3.jpg", 
  "/img/h4.jpg", 
  "/img/h5.jpg", 
  "/img/h6.jpg"
];
---

<section class="hero-section">
  <div class="swiper heroSwiper">
    <div class="swiper-wrapper">
      {images.map((src) => (
        <div class="swiper-slide">
          <div class="image-container">
            <img src={src} class="ken-burns-img" alt="Thailand Folklore Heritage" />
          </div>
        </div>
      ))}
    </div>

    <div class="swiper-button-prev luxury-arrow"></div>
    <div class="swiper-button-next luxury-arrow"></div>

    <div class="swiper-pagination luxury-pagination"></div>
  </div>

  <div class="hero-overlay-content">
    <p class="hero-tag">THE SOVEREIGN DIGITAL HERITAGE</p>
    <h1 class="hero-title">Thailand <span class="gold-text">Folklore</span></h1>
  </div>
</section>

<script is:inline>
  // ฟังก์ชันเริ่มต้น Swiper เมื่อหน้าเว็บพร้อม
  function initHeroSlider() {
    new Swiper(".heroSwiper", {
      loop: true,
      speed: 2500,
      effect: 'fade', // การเปลี่ยนภาพแบบจางนุ่มนวล
      fadeEffect: { crossFade: true },
      autoplay: {
        delay: 5000,
        disableOnInteraction: false,
      },
      navigation: {
        nextEl: ".swiper-button-next",
        prevEl: ".swiper-button-prev",
      },
      pagination: {
        el: ".swiper-pagination",
        clickable: true,
      },
      // ป้องกันภาพแหลมออกด้านข้าง
      watchSlidesProgress: true,
      observer: true,
      observeParents: true,
    });
  }

  // รันคำสั่ง
  if (document.readyState === 'complete') {
    initHeroSlider();
  } else {
    window.addEventListener('load', initHeroSlider);
  }
</script>

<style>
  /* 🏗️ Layout & Fullscreen */
  .hero-section {
    position: relative;
    width: 100vw;
    height: 100vh;
    overflow: hidden;
    background: #000;
  }

  .swiper {
    width: 100%;
    height: 100%;
  }

  .image-container {
    width: 100%;
    height: 100%;
    overflow: hidden;
  }

  /* ✨ Ken Burns Effect (ซูมเข้า-ออก) */
  .ken-burns-img {
    width: 100%;
    height: 100%;
    object-fit: cover;
    animation: kenburns-effect 20s ease-in-out infinite alternate;
  }

  @keyframes kenburns-effect {
    from { transform: scale(1); }
    to { transform: scale(1.15); }
  }

  /* 🏛️ Overlay Content */
  .hero-overlay-content {
    position: absolute;
    inset: 0;
    z-index: 10;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    color: #fff;
    text-align: center;
    background: rgba(0, 0, 0, 0.3); /* ม่านดำจางๆ ให้ข้อความชัด */
    pointer-events: none; /* ให้คลิกทะลุไปโดนปุ่มสไลด์ได้ */
  }

  .hero-tag {
    font-size: 12px;
    letter-spacing: 0.8em;
    color: #bf953f;
    font-weight: bold;
    margin-bottom: 20px;
    text-transform: uppercase;
  }

  .hero-title {
    font-size: clamp(3rem, 10vw, 7.5rem);
    font-family: serif;
    font-style: italic;
    margin: 0;
    line-height: 1;
  }

  .gold-text {
    color: #bf953f;
  }

  /* 🧭 Luxury Interactive Arrows (ลูกศรวงกลม) */
  .luxury-arrow {
    width: 60px !important;
    height: 60px !important;
    border: 1px solid rgba(191, 149, 63, 0.6);
    border-radius: 50%;
    color: #bf953f !important;
    background: rgba(0, 0, 0, 0.2);
    transition: all 0.4s ease;
    z-index: 20;
  }

  .luxury-arrow:hover {
    background: #bf953f;
    color: #fff !important;
    border-color: #bf953f;
    box-shadow: 0 0 20px rgba(191, 149, 63, 0.4);
    transform: scale(1.1);
  }

  .luxury-arrow::after {
    font-size: 20px !important;
    font-weight: bold;
  }

  /* 🔢 Luxury Pagination (วงกลมบอกลำดับภาพ) */
  :global(.luxury-pagination) {
    bottom: 40px !important;
    z-index: 20 !important;
  }

  :global(.luxury-pagination .swiper-pagination-bullet) {
    width: 12px !important;
    height: 12px !important;
    background: transparent !important;
    border: 1.5px solid rgba(255, 255, 255, 0.7) !important;
    opacity: 1 !important;
    margin: 0 10px !important;
    transition: all 0.4s ease;
  }

  :global(.luxury-pagination .swiper-pagination-bullet-active) {
    background: #bf953f !important;
    border-color: #bf953f !important;
    transform: scale(1.4);
    box-shadow: 0 0 15px rgba(191, 149, 63, 0.6);
  }
</style>