---
const images = ["/img/h1.jpg", "/img/h2.jpg"];
---
<section class="reborn-v32">
    <div class="test-panel">
        <h1 class="blink">V32 - FULL RESET</h1>
        <p>IF YOU SEE THIS, THE SYSTEM IS LIVE</p>
    </div>
    <div class="debug-images">
        <img src={images[0]} alt="Test" />
    </div>
</section>

<style>
    /* 🛡️ บังคับล้าง Cache ด้วย Class Name ใหม่ */
    .reborn-v32 {
        width: 100vw;
        height: 100vh;
        background: #bf953f; /* เปลี่ยนเป็นสีทองทั้งหน้าจอเพื่อให้เห็นความต่างชัดเจน */
        display: flex;
        align-items: center;
        justify-content: center;
        position: relative;
        z-index: 1000;
    }
    .test-panel {
        text-align: center;
        color: white;
        z-index: 1001;
    }
    .blink {
        font-size: 5rem;
        animation: blink-anim 1s infinite;
    }
    @keyframes blink-anim {
        0%, 100% { opacity: 1; }
        50% { opacity: 0.3; }
    }
    .debug-images {
        position: absolute;
        bottom: 20px;
        width: 200px;
        border: 5px solid white;
    }
    img { width: 100%; height: auto; }
</style>