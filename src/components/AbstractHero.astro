---
// เรียกไฟล์ h1.jpg - h6.jpg จากโฟลเดอร์ public/img/ ตามที่คุณยืนยัน
const images = [
  "/img/h1.jpg", "/img/h2.jpg", "/img/h3.jpg", 
  "/img/h4.jpg", "/img/h5.jpg", "/img/h6.jpg"
];
---

<section class="hero-zoom-container">
  <div class="zoom-slider">
    {images.map((src, i) => (
      <div class="slide" style={`animation-delay: ${i * 6}s;`}>
        <div class="image-wrapper">
          <img src={src} alt="Heritage Artifact" />
        </div>
      </div>
    ))}
  </div>

  <div class="hero-overlay">
    <div class="content-box">
      <p class="hero-tag">THE SOVEREIGN DIGITAL HERITAGE</p>
      <h1 class="hero-title font-serif">Thailand <span class="gold-text">Folklore</span></h1>
      <div class="hero-divider"></div>
      <p class="hero-desc">Preserving the spirit of Lanna and the soul of Thailand</p>
    </div>
  </div>
</section>

<style>
  .hero-zoom-container {
    position: relative;
    width: 100%;
    height: 100vh; /* เต็มหน้าจอแบบเดิม */
    overflow: hidden;
    background: #000;
  }

  .zoom-slider {
    position: absolute;
    inset: 0;
    z-index: 1;
  }

  .slide {
    position: absolute;
    inset: 0;
    opacity: 0;
    animation: zoomAndFade 36s linear infinite; /* วนลูปภาพละ 6 วินาที */
  }

  .image-wrapper {
    width: 100%;
    height: 100%;
    transform: scale(1.1); /* เริ่มต้นที่ซูมเล็กน้อย */
  }

  .slide img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  /* ✨ Ken Burns Effect (การซูมที่นุ่มนวล) */
  @keyframes zoomAndFade {
    0% { opacity: 0; transform: scale(1); }
    5% { opacity: 1; }
    16.66% { opacity: 1; }
    22% { opacity: 0; transform: scale(1.15); }
    100% { opacity: 0; }
  }

  /* 🏛️ Styling ส่วนข้อความ */
  .hero-overlay {
    position: absolute;
    inset: 0;
    z-index: 10;
    display: flex;
    align-items: center;
    justify-content: center;
    background: radial-gradient(circle at center, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0.6) 100%);
    text-align: center;
    color: #fff;
    padding: 0 20px;
  }

  .hero-tag { font-size: 12px; letter-spacing: 0.8em; color: #bf953f; font-weight: bold; margin-bottom: 20px; }
  .hero-title { font-size: clamp(4rem, 12vw, 8rem); line-height: 1; margin: 0; text-shadow: 0 10px 30px rgba(0,0,0,0.5); }
  .gold-text { color: #bf953f; font-style: italic; }
  
  .hero-divider { width: 80px; height: 2px; background: #bf953f; margin: 30px auto; }
  .hero-desc { font-size: 18px; letter-spacing: 0.2em; opacity: 0.8; font-style: italic; }

  @media (max-width: 768px) {
    .hero-title { font-size: 3.5rem; }
  }
</style>